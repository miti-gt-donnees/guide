image: pnavaro/pandoc

pages:
  stage: deploy
  script:
    - make
    - mkdir -p public
    - cp synthese_2020.pdf public/
    - apt-get install -yq mkdocs
    - make html
  tags:
    - pandoc, docker
  artifacts:
    paths:
      - public
